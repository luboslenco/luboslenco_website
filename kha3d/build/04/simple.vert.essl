#version 100
precision mediump float;
uniform mat4 MVP;
attribute vec3 pos;
varying vec3 fragmentColor;
attribute vec3 col;


void main()
{
	gl_Position = (MVP * vec4(pos[0], pos[1], pos[2], 1.0));
	fragmentColor = col;
	// Branch to 6
	// Label 6
	return;
}

