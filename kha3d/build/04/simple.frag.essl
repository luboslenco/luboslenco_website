#version 100
precision mediump float;
varying vec3 fragmentColor;


void main()
{
	gl_FragColor = vec4(fragmentColor[0], fragmentColor[1], fragmentColor[2], 1.0);
	// Branch to 6
	// Label 6
	return;
}

