#version 100
precision mediump float;
uniform sampler2D tex;
varying vec2 texCoord;
varying vec3 norm;


void main()
{
	gl_FragColor = texture2D(tex, texCoord);
	// Merge 26 0
	if ((gl_FragColor[3] <= 0.5)) // true: 25 false: 26
	{ // Label 25
		discard;
	} // Label 26
	// Branch to 6
	// Label 6
	return;
}

